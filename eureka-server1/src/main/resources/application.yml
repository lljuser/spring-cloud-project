server:
  port: 8761
eureka:
  instance:
    metadata-map:
      zone: zone1
    #hostname: localhost
    prefer-ip-address: true
    ip-address: 127.0.0.1
  client:
    #register-with-eureka: false
    #fetch-registry: false
    service-url:
      #defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
      defaultZone: http://eureka:eureka@0.0.0.0:8762/eureka/
spring:
  application:
    name: eureka-server
  security:
    user:
      name: eureka
      password: eureka


