server:
  port: 8091
eureka:
  instance:
    metadata-map:
      zone: zone2
    prefer-ip-address: true
    ip-address: 127.0.0.1
    lease-renewal-interval-in-seconds: 10
  client:
    service-url:
      defaultZone: http://eureka:eureka@127.0.0.1:8762/eureka/
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://10.1.1.223/llj/spring-cloud-config-center.git
          username: llj
          password: lljlljllj
        health:
          repositories:
            webapp-server:
              name: web-appserver
              label: master
              profiles: dev
  security:
    user:
      name: config
      password: config
###encrypt some config info
encrypt:
  key: thisismykey
