server:
  port: 8888

###-----1 client---------
eureka:
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
  client:
    service-url:
      defaultZone: http://eureka:eureka@10.1.1.227:8761/eureka/
spring:
  application:
    name: webapp
logging:
  level:
    com.example.webapp.service.UserFeignClient: DEBUG

#webapp-server:
#  ribbon:
    ###-----client-----config server list for old project  no eureka only use ribbon
    #listOfServers: localhost:8080,localhost:8001
    #NIWSServerListClassName: com.netflix.loadbalancer.ConfigurationBasedServerList
    #NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule

feign:
  client:
    config:
      webapp-server:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full
#        errorDecoder: com.example.SimpleErrorDecoder
#        retryer: com.example.SimpleRetryer
#        requestInterceptors:
#          - com.example.FooRequestInterceptor
#          - com.example.BarRequestInterceptor
#        decode404: false
#        encoder: com.example.SimpleEncoder
#        decoder: com.example.SimpleDecoder
#        contract: com.example.SimpleContract

feign:compression.request.enabled: true
feign.compression.response.enabled: true
###if hystrix in classpath then fegin will use hystrix. you can disable hystrix by below
#feign.hystrix.enabled: false


